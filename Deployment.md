# Deployment Documentation

## Overview

This document describes the deployment architecture and CI/CD workflow used to deploy the application on AWS.

> **Note:** The application source code is **not my own implementation**. It was cloned from an existing repository for learning and infrastructure automation purposes. My primary contribution was designing, configuring, and deploying the complete AWS infrastructure and CI/CD pipeline.
>
> **Original Repository:**
> *https://github.com/hkhcoder/vprofile-project*

---

# AWS Services Used

| Service                    | Purpose                                          |
| -------------------------- | ------------------------------------------------ |
| Amazon EC2                 | Build environment for AWS CodeBuild              |
| AWS Elastic Beanstalk      | Application deployment and hosting               |
| Amazon RDS                 | Managed relational database                      |
| Amazon S3                  | Artifact storage                                 |
| AWS CodePipeline           | Continuous Integration and Continuous Deployment |
| AWS CodeBuild              | Application build process                        |
| Amazon EC2 Security Groups | Network access control                           |
| Bitbucket                  | Source code repository                           |

---

# Deployment Architecture

```
Developer
      │
      ▼
 Bitbucket Repository
      │
      ▼
 AWS CodePipeline
      │
      ▼
 AWS CodeBuild
(Build + Environment Variables)
      │
      ▼
 Store Build Artifact in Amazon S3
      │
      ▼
 AWS Elastic Beanstalk
      │
      ▼
 Application
      │
      ▼
 Amazon RDS
```

---

# Deployment Workflow

1. A developer pushes changes to the Bitbucket repository.
2. AWS CodePipeline detects the new commit.
3. CodePipeline retrieves the latest source code.
4. AWS CodeBuild starts the build process.
5. CodeBuild executes the commands defined in `build.yml`.
6. Application dependencies are installed and the application is built.
7. The deployment artifact is generated.
8. The artifact is stored in Amazon S3.
9. AWS Elastic Beanstalk retrieves and deploys the new application version.
10. Elastic Beanstalk performs health checks on the deployed environment.
11. The environment status is monitored to verify that the application is healthy.
12. Deployment logs and status can be reviewed through CodePipeline, CodeBuild, and Elastic Beanstalk.

---

## 2. Continuous Integration

AWS CodePipeline continuously monitors the repository.

When a new commit is detected, the pipeline starts automatically.

Pipeline stages include:

* Source
* Build
* Deploy

---

## 3. Build Stage

AWS CodeBuild performs the application build.

During this stage:

* Downloads the source code
* Installs project dependencies
* Executes the build process
* Uses environment variables configured inside CodeBuild
* Packages the deployment artifact

The generated artifact is uploaded to Amazon S3.

---
## Build Configuration

The AWS CodeBuild process is configured using a `buildspec.yml` file.

The `buildspec.yml` file defines the commands and configuration required to build and package the application.

It is responsible for:

* Installing the required dependencies
* Preparing the application for deployment
* Running the required build commands
* Defining the build output/artifacts
* Preparing the deployment package for Elastic Beanstalk

The build configuration is version-controlled together with the project source code, allowing the build process to be reproduced consistently across deployments.

Example structure:

```yaml
version: 0.2

phases:
  install:
    # Install dependencies

  build:
    # Build application

artifacts:
  # Define deployment artifacts
```

> The exact commands and phases depend on the application's technology stack.
--

## 4. Artifact Storage

Amazon S3 is used as the artifact repository.

The build output generated by CodeBuild is stored in S3 before deployment.

This separates the build process from the deployment process and provides a centralized location for deployment packages.

---

## 5. Deployment

AWS Elastic Beanstalk retrieves the latest artifact from the deployment pipeline.

Elastic Beanstalk handles:

* Environment provisioning
* Application deployment
* Version management
* Rolling updates
* Health monitoring

---
## Application Health Checks

Health checks are used to verify that the deployed application is running correctly after deployment.

Elastic Beanstalk continuously monitors the health of the application environment and reports its current status.

The health status helps verify:

* Application availability
* Instance health
* Application response
* Deployment status
* Potential application or infrastructure failures

After a successful deployment, the Elastic Beanstalk environment is checked to ensure that the application reaches a healthy state before considering the deployment successful.

This provides an additional validation layer after the deployment process.
---

## 6. Database

Amazon RDS is used as the application's managed relational database.

The application connects to the RDS instance using environment configuration variables.

---

## 7. Security

Security Groups were configured to control inbound and outbound traffic.

Examples include:

* Custom TCP (3306)
* SSH (22) 
* Database access restricted to the application environment

---

# Environment Variables

Application configuration is managed through AWS CodeBuild environment variables and deployment environment configuration.

Typical variables include:

* Database endpoint
* Database username
* Database password
* Application environment
* AWS Region
* Other application-specific configuration

> Sensitive information is never committed to the repository.

---

# Deployment Steps

1. Push changes to Bitbucket.
2. AWS CodePipeline detects the new commit.
3. CodeBuild starts automatically.
4. The application is built.
5. Build artifacts are uploaded to Amazon S3.
6. Elastic Beanstalk deploys the latest application version.
7. The application becomes available after a successful deployment.

---

# Monitoring

Deployment status can be monitored using:

* AWS CodePipeline
* AWS CodeBuild Logs
* Elastic Beanstalk Events
* Enviroment Logs

---

# Infrastructure Components

* Amazon EC2
* AWS Elastic Beanstalk
* Amazon RDS
* Amazon S3
* AWS CodePipeline
* AWS CodeBuild
* Amazon EC2 Security Groups
* Bitbucket

---

# Security Considerations

* Sensitive values are stored as environment variables.
* Security Groups restrict unnecessary access.
* Database is not publicly exposed.
* Build artifacts are stored securely in Amazon S3.

---

# Future Improvements

Potential enhancements include:

* Infrastructure as Code using Terraform
* AWS Secrets Manager for secret management
* Automated testing before deployment
* Blue/Green deployments
* CloudWatch alarms and notifications
* Application Load Balancer with Auto Scaling
* AWS WAF for additional application security

---

# Conclusion

This project demonstrates a complete AWS deployment workflow using a managed CI/CD pipeline. The focus of this project is the deployment process, cloud infrastructure configuration, build automation, artifact management, and application hosting rather than application development.
